{
  "name": "copilot-doctor",
  "version": "0.1.5",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && cp popup.html options.html jobs.html popup.css options.css jobs.css manifest.json dist/ && cp -r icons/ dist/",
    "watch": "tsc --watch",
    "pack": "npm run build && node scripts/pack.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "archiver": "^8.0.0",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
